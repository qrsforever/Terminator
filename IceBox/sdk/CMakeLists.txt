INCLUDE (${CMAKE_CURRENT_LIST_DIR}/${ICEBOX_MODEL}/CMakeLists.txt)

SET (CMAKE_C_COMPILER   "${PLATFORM_CROSS_COMPILER}gcc")
SET (CMAKE_CXX_COMPILER "${PLATFORM_CROSS_COMPILER}g++")
SET (CMAKE_AR           "${PLATFORM_CROSS_COMPILER}ar")
SET (CMAKE_STRIP        "${PLATFORM_CROSS_COMPILER}strip")
SET (CMAKE_LINKER       "${PLATFORM_CROSS_COMPILER}ld")
SET (CMAKE_NM           "${PLATFORM_CROSS_COMPILER}nm")
SET (CMAKE_OBJCOPY      "${PLATFORM_CROSS_COMPILER}objcopy")
SET (CMAKE_OBJDUMP      "${PLATFORM_CROSS_COMPILER}objdump")
SET (CMAKE_RANLIB       "${PLATFORM_CROSS_COMPILER}ranlib")

LIST (APPEND INCLUDES 
    ${PLATFORM_SDK_PATH}/include
    ${PLATFORM_SDK_PATH}/include/sdk
    ${PLATFORM_SDK_PATH}/include/sdk/utility
    )

LIST (APPEND LIBS_DIRECTORIES
    ${PLATFORM_SDK_PATH}/rootfs/lib
    ${PLATFORM_SDK_PATH}/rootfs/usr/lib
    ${PLATFORM_SDK_PATH}/rootfs/usr/local/lib
    ${PLATFORM_SDK_PATH}/rootfs/home/${PLATFORM_HOME}/lib/
    )
