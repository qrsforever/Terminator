LIST (APPEND INCLUDES 
    ${CMAKE_CURRENT_LIST_DIR}/include
    ${CMAKE_CURRENT_LIST_DIR}/include/freetype
    )

LIST (APPEND LIBS_DIRECTORIES
    # ${CMAKE_CURRENT_LIST_DIR}/lib
    )

LIST (APPEND LIBRARIES 
    # freetype
    )    
