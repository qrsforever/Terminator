LIST (APPEND INCLUDES ${CMAKE_CURRENT_LIST_DIR})

LIST (APPEND CLOUD_C_SRCS
    ${CMAKE_CURRENT_LIST_DIR}/sntpc.c
    )

LIST (APPEND CLOUD_CXX_SRCS
    ${CMAKE_CURRENT_LIST_DIR}/NTPSyncTask.cpp
    )
