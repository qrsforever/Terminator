SET (ICEBOX_MODEL "$ENV{ICEBOX_MODEL}" CACHE STRING "RO product model")
SET (ICEBOX_CHIPSET "$ENV{ICEBOX_CHIPSET}" CACHE STRING  "RO product chipset")
SET (ICEBOX_PRODUCT "$ENV{PRODUCT_CONFIG}" CACHE STRING "RO product config")

SET (EXTERNAL_LIBS_GTAR "$ENV{EXTERNAL_LIBS_GTAR}" CACHE STRING "RO external libs tarball")
SET (PLATFORM_LIBS_GTAR "$ENV{PLATFORM_LIBS_GTAR}" CACHE STRING "RO platform libs tarball")
